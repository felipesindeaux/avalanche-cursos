from django.urls import path

from . import views

urlpatterns = [
    # path("courses/<course_id>/lessons/tasks/", views.ListCreateTaskView.as_view()),
    # path(
    #     "courses/<course_id>/lessons/<lesson_id>/tasks/",
    #     views.RetrieveUpdateDeleteLessonView.as_view(),
    # ),
]
